
function menu_toggle(){ 
    menu = document.getElementById("navig");
    if ( menu.classList.contains("hide")){
        menu.classList.toggle("hide");
    }
    else{
        menu.classList.toggle("hide");
    }
}